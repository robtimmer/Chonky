/**
 * We have option IDs in a separate file to avoid circular deps...
 */
export declare const OptionIds: {
    ShowHiddenFiles: string;
    ShowFoldersFirst: string;
    DarkMode: string;
};
